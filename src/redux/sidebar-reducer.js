let initialState = {
    myFriends: [
        {id: '1', name: 'Alex'},
        {id: '2', name: 'Romeo'},
        {id: '3', name: 'Nik'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;

}

export default sidebarReducer;